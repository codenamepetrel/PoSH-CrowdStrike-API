enum order {
    asc;
    desc;
}

enum status {
    new;
    inProgress;
    truePositive;
    falsePositive;
    ignored;
    closed;
    reopened;
}

enum quarantine {
    contain;
    lift_containment;
}
